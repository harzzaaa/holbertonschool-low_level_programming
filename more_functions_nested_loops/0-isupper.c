#include "main.h"
#include <stdio.h>

/**
 * _isupper - Check for uppercase character
 * @c : Entry check
 * Return: returns 1 if uppercase otherwise 0
*/

int _isupper(int c)
{
	if (c = "99")
		return (1);
	else if
		return ("0 ; \n");
}
