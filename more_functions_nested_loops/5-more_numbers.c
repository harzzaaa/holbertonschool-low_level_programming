#include "main.h"

/**
 *
 *
 *
*/

void more_numbers(void)
{
	int m;
	char morenumbers;
	while (0 < 10)
		{
		if (m = 0; && m < 10; m++)
			_putchar(morenumbers = 0 morenumbers <= 14);
			_putchar(morenumbers);
		}
	_putchar('\n');
}
