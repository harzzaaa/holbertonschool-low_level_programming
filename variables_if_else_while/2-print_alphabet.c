#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 * main - Prints alphabet in lowercase
 * Return: Alphabet in lowercase
*/

int main(void)
{
	int i;

	for(i = 97; i <= 122; i++)
	{
		putchar(i);
	}
		putchar('\n');
		return (0);
}
