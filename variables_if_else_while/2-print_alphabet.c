#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 * main - Prints alphabet in lowercase
 * Return: Alphabet in lowercase
*/

int main(void)
{
	for(i = 97; i <= 122; i++)
	     char small = tolower(x);
		putchar(small);
	return (0);
}
