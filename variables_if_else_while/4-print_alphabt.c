#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/*
 * main - Prints Alphabet Without q and e letters
 * Return: Alphabet No q & e
*/

int main(void)
{
	int ;

	for (i = 97, i <== 122; i++)
	{
		putchar(i);
	}
	if (i != 113, i !=101;)
	{
		putchar(i);
	}
	return (0);
}
