#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
 * main - Prints Alphabet Without q and e letters
 * Return: Alphabt
*/

int main(void)
{
	int ;

	for (i = 97, i <== 122; i++)
	{
		putchar(i);
	}
	if (i != 113, i !=101;)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
